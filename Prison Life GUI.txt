_G.autoKeyCard = false

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    
    local Window = Library.CreateLib("Skiddie Prison Life GUI", "DarkTheme")
    
    local Tab = Window:NewTab("Prison Life")
    
    local Section = Tab:NewSection("Weapons")
    
    local Section2 = Tab:NewSection("Movement")
    
    local Section3 = Tab:NewSection("Collection")

    local Section4 = Tab:NewSection("OP")
    
    Section4:NewButton("Ultimate Trolling GUI", "Gives the UTG from Flamingo", function()
    print("Clicked")
    game.Players.LocalPlayer:Kick("You Are An Idiot")
    end)
    
    Section:NewDropdown("Give Gun", "Gives the localplayer a gun", {"M9", "Remington 870", "AK-47"}, function(v)
        local A_1 = game:GetService("Workspace")["Prison_ITEMS"].giver[v].ITEMPICKUP
        local Event = game:GetService("Workspace").Remote.ItemHandler
        Event:InvokeServer(A_1)
    end)

    Section2:NewSlider("Walk Speed", "Increases your walkspeed, dumbass", 500, 0, function(s) -- 500 (MaxValue) | 0 (MinValue)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
    end)
    
    Section2:NewSlider("Jump Power", "Increases your jumppower, dumbass", 500, 0, function(s) -- 500 (MaxValue) | 0 (MinValue)
        game.Players.LocalPlayer.Character.Humanoid.JumpPower = s
    end)
    
    Section3:NewToggle("Auto Keycard", "Collects all keycards around the map", function(state)
        _G.autoKeyCard = state
        print("Auto Keycard is: ", state)
        if state then
            autoKeyCard()
        end
    end)
    
    Section:NewLabel("Press K to Enable KillAura")
    
    Section:NewDropdown("Gun Mod", "Makes the gun op", {"M9", "Remington 870", "AK-47"}, function(v)
        local module = nil
        if game:GetService("Players").LocalPlayer.Backpack:FindFirstChild(v) then
            module = require(game:GetService("Players").LocalPlayer.Backpack[v].GunStates)
        elseif game:GetService("Players").LocalPlayer.Character:FindFirstChild(v) then
            module = require(game:GetService("Players").LocalPlayer.Character[v].GunStates)
        end
        if module ~= nil then
            module["MaxAmmo"] = math.huge
            module["CurrentAmmo"] = math.huge
            module["StoredAmmo"] = math.huge
            module["FireRate"] = 0.001
            module["Spread"] = 0
            module["Range"] = math.huge
            module["Bullets"] = 10
            module["ReloadTime"] = 0.000001
            module["AutoFire"] = true
        end
    

    
    function autoKeyCard()
        while _G.autoKeyCard == true do
                -- Script generated by SimpleSpy - credits to exx#9394
        
            local args = {
                [1] = workspace.Prison_ITEMS.single:FindFirstChild("Key card").ITEMPICKUP
            }
            
                workspace.Remote.ItemHandler:InvokeServer(unpack(args))

         end
    end
    
    function tpgui()    
        loadstring(game:HttpGet("https://pastebin.com/raw/8nsJTf3J", true))();
    end
end)

mainRemotes = game.ReplicatedStorage
meleeRemote = mainRemotes['meleeEvent']

killAura = true

contextactionservice = game.ContextActionService

function dumbass()
    game.Players.Localplayer:Kick("You Are An Idiot.exe")
end

function toggleKillAura(actionName, inputState, inputObject)
print('Doing the action : ' .. actionName)
if inputState == Enum.UserInputState.Begin then
if killAura == true then
killAura = false
else
killAura = true
end
end
end

contextactionservice:BindAction('ToggleKillAura', toggleKillAura, false, Enum.KeyCode.K)

while wait() do
if killAura == true then
for _, plr in pairs (game:GetService('Players'):GetChildren()) do
if plr.Name ~= game.Players.LocalPlayer.Name then
meleeRemote:FireServer(plr)
end
end
end
end
